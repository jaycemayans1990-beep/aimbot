-- 1. Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")

local localPlayer = Players.LocalPlayer
local espCache = {}

-- Camera Caching
local camera = workspace.CurrentCamera
workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
    camera = workspace.CurrentCamera
end)

-- 2. Global States
local State = {
    Aimbot = true,
    TeamCheck = true,
    WallCheck = false,
    Prediction = true,
    AutoBulletSpeed = true,
    BulletSpeed = 5000,
    BulletGravity = 0,
    Smoothness = 0.4,
    FOV = 100,
    ShowFOV = false,
    ESP = true,
    ESPName = false
}

local currentTarget = nil
local cachedBulletSpeed = 2000
local cachedBulletGravity = 0 

-- 3. Safe Ping Helper Function
local function getPing()
    if localPlayer then
        local success, ping = pcall(function()
            return localPlayer:GetNetworkPing()
        end)
        if success and typeof(ping) == "number" then
            return ping
        end
    end

    local successStats, statsPing = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue() / 1000
    end)
    if successStats and typeof(statsPing) == "number" then
        return statsPing
    end

    return 0.05
end

-- 4. Dynamic Camera Helper
local function getCamera()
    return camera
end

-- 5. Dynamic Weapon Stats Detection System
local function updateWeaponStats()
    if not State.AutoBulletSpeed then
        cachedBulletSpeed = math.max(State.BulletSpeed or 2000, 1)
        cachedBulletGravity = State.BulletGravity or 0
        return
    end

    local char = localPlayer and localPlayer.Character
    local tool = char and char:FindFirstChildOfClass("Tool")
    
    if tool then
        local toolName = tool.Name:lower()
        local isLauncher = toolName:find("rpg") or toolName:find("rocket") or toolName:find("launcher") or toolName:find("grenade") or toolName:find("bazooka")

        local acsSettings = tool:FindFirstChild("ACS_Settings") or tool:FindFirstChild("Settings") or tool:FindFirstChild("Config")
        if acsSettings and acsSettings:IsA("ModuleScript") then
            local success, config = pcall(require, acsSettings)
            if success and type(config) == "table" then
                local speed = config.Velo or config.MuzzleVelocity or config.BulletSpeed or config.Velocity
                if type(speed) == "number" and speed > 0 then
                    cachedBulletSpeed = speed
                end
                
                local drop = config.Drop or config.BulletDrop or config.Gravity or config.G
                if type(drop) == "number" then
                    cachedBulletGravity = drop
                else
                    cachedBulletGravity = isLauncher and workspace.Gravity or 0
                end
                return
            end
        end

        local foundSpeed = false
        for name, value in pairs(tool:GetAttributes()) do
            local lower = name:lower()
            if (lower:find("speed") or lower:find("velocity") or lower:find("velo")) and type(value) == "number" then
                cachedBulletSpeed = value
                foundSpeed = true
            end
        end

        if foundSpeed then
            cachedBulletGravity = isLauncher and workspace.Gravity or 0
            return
        end

        if isLauncher then
            cachedBulletSpeed = 400
            cachedBulletGravity = workspace.Gravity
            return
        end
    end

    cachedBulletSpeed = math.max(State.BulletSpeed or 2000, 1)
    cachedBulletGravity = 0
end

local function setupCharacterSpeedListener(char)
    if not char then return end
    char.ChildAdded:Connect(function(child)
        if child:IsA("Tool") then
            task.defer(updateWeaponStats)
        end
    end)
    char.ChildRemoved:Connect(function(child)
        if child:IsA("Tool") then
            task.defer(updateWeaponStats)
        end
    end)
    updateWeaponStats()
end

if localPlayer then
    if localPlayer.Character then setupCharacterSpeedListener(localPlayer.Character) end
    localPlayer.CharacterAdded:Connect(setupCharacterSpeedListener)
end

-- 6. Safe FOV Circle Engine
local FOVCircle = nil
if typeof(Drawing) == "table" and typeof(Drawing.new) == "function" then
    pcall(function()
        local circle = Drawing.new("Circle")
        circle.Thickness = 1.5
        circle.Color = Color3.fromRGB(255, 255, 255)
        circle.Filled = false
        circle.Transparency = 0.7
        circle.Visible = false
        FOVCircle = circle
    end)
end

-- 7. Target Validation Check
local function isValidEnemy(player)
    if not player or not localPlayer or player == localPlayer or not player.Character then 
        return false 
    end
    
    local humanoid = player.Character:FindFirstChildOfClass("Humanoid")  
    local rootPart = player.Character:FindFirstChild("HumanoidRootPart")  
      
    if not humanoid or not rootPart or humanoid.Health <= 0 then   
        return false   
    end  
    
    if player.Character:FindFirstChildOfClass("ForceField") then  
        return false  
    end  
    
    if State.TeamCheck then  
        local myTeam = localPlayer.Team
        local targetTeam = player.Team
        if myTeam ~= nil and targetTeam ~= nil and myTeam == targetTeam then  
            return false  
        end  
    end  
    
    return true
end

-- 8. Wall Check Function
local raycastParams = RaycastParams.new()
raycastParams.FilterType = Enum.RaycastFilterType.Exclude
raycastParams.IgnoreWater = true
local filterList = {}

local function isVisible(targetPart)
    if not State.WallCheck then return true end
    if not targetPart or not targetPart.Parent then return false end

    local character = localPlayer and localPlayer.Character
    if not character then return false end

    local cam = getCamera()
    if not cam then return false end

    filterList[1] = character
    raycastParams.FilterDescendantsInstances = filterList

    local origin = cam.CFrame.Position
    local destination = targetPart.Position
    local direction = destination - origin

    local result = workspace:Raycast(origin, direction, raycastParams)

    if result then
        local targetModel = targetPart:FindFirstAncestorOfClass("Model")
        if targetModel and result.Instance:IsDescendantOf(targetModel) then
            return true
        end
        return false
    end

    return true
end

-- 9. Target Acquisition
local function getClosestTarget()
    local closestPlayer = nil
    local shortestDist = State.FOV
    local cam = getCamera()
    if not cam then return nil end
    local screenCenter = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)
    
    for _, player in ipairs(Players:GetPlayers()) do  
        if isValidEnemy(player) then  
            local targetPart = player.Character:FindFirstChild("Head") or player.Character:FindFirstChild("HumanoidRootPart")  
            if targetPart then  
                local screenPoint, onScreen = cam:WorldToViewportPoint(targetPart.Position)  
                  
                if onScreen then  
                    local dist = (Vector2.new(screenPoint.X, screenPoint.Y) - screenCenter).Magnitude
                    local effectiveDist = (player == currentTarget) and (dist * 0.85) or dist
                    
                    if effectiveDist < shortestDist then  
                        if isVisible(targetPart) then
                            shortestDist = effectiveDist  
                            closestPlayer = player  
                        end
                    end  
                end  
            end  
        end  
    end  
    return closestPlayer
end

-- 10. Physics Dynamic Prediction Engine (Compatibility & Anti-Nil Safe)
local velocityHistory = setmetatable({}, {__mode = "k"})
local lastTickTime = setmetatable({}, {__mode = "k"})

local function getPredictedPosition(target, bulletSpeed, bulletGravity, pingOffsetSec)
    if not target then return nil end
    local char = target.Character
    if not char then return nil end

    local rootPart = char:FindFirstChild("HumanoidRootPart")
    local headPart = char:FindFirstChild("Head") or rootPart
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not rootPart or not headPart then return nil end

    local cam = workspace.CurrentCamera
    if not cam then return headPart.Position end

    local targetPos = headPart.Position
    if not State.Prediction then return targetPos end

    local camPos = cam.CFrame.Position
    local v = math.max(bulletSpeed or cachedBulletSpeed or 3500, 1)
    local g = bulletGravity or cachedBulletGravity or 0

    local rawPing = getPing() or 0
    local pingInSec = (rawPing > 5) and (rawPing / 1000.0) or rawPing
    local pingSec = pingOffsetSec or (pingInSec + 0.0166)

    local now = os.clock()
    local lastTime = lastTickTime[target] or (now - 0.016)
    local dt = math.clamp(now - lastTime, 0.001, 0.1)
    lastTickTime[target] = now

    if not velocityHistory[target] then velocityHistory[target] = {} end
    local hist = velocityHistory[target]

    local rawVel = rootPart.AssemblyLinearVelocity
    local currentVel = Vector3.new(rawVel.X, math.clamp(rawVel.Y, -80, 80), rawVel.Z)

    table.insert(hist, currentVel)
    if #hist > 5 then table.remove(hist, 1) end

    local avgVel = Vector3.new(0, 0, 0)
    for _, vel in ipairs(hist) do
        avgVel = avgVel + vel
    end
    avgVel = avgVel / #hist

    local targetAccel = Vector3.new(0, 0, 0)
    if #hist >= 2 then
        local prevVel = hist[#hist - 1]
        targetAccel = (currentVel - prevVel) / dt
        if targetAccel.Magnitude > 40 then
            targetAccel = targetAccel.Unit * 40
        end
    elseif humanoid then
        local state = humanoid:GetState()
        local isAir = (state == Enum.HumanoidStateType.Freefall or
                       state == Enum.HumanoidStateType.Jumping or
                       humanoid.FloorMaterial == Enum.Material.Air)
        if isAir then
            targetAccel = Vector3.new(0, -g, 0)
        end
    end

    if g < 0.01 then
        local predictedPos = targetPos
        for iter = 1, 4 do
            local dist = (predictedPos - camPos).Magnitude
            local tof = (dist / v) + pingSec
            predictedPos = targetPos + (avgVel * tof) + (0.5 * targetAccel * (tof * tof))
        end
        return predictedPos
    end

    local predictedPos = targetPos
    local lastTanTheta = 0

    for iter = 1, 6 do
        local diff = predictedPos - camPos
        local flatDiff = Vector3.new(diff.X, 0, diff.Z)
        local horizDist = flatDiff.Magnitude
        local vertDist = diff.Y

        if horizDist < 0.001 then
            local tofVert = math.abs(vertDist) / math.max(v, 0.001) + pingSec
            predictedPos = targetPos + avgVel * tofVert + 0.5 * targetAccel * (tofVert * tofVert)

-- 10. Physics Dynamic Prediction Engine (Full Accuracy + High Precision)
local velocityHistory = setmetatable({}, {__mode = "k"})
local lastTickTime = setmetatable({}, {__mode = "k"})

local function getPredictedPosition(target, bulletSpeed, bulletGravity, pingOffsetSec)
    if not target then return nil end
    local char = target.Character
    if not char then return nil end

    local rootPart = char:FindFirstChild("HumanoidRootPart")
    local headPart = char:FindFirstChild("Head") or rootPart
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not rootPart or not headPart then return nil end

    local cam = workspace.CurrentCamera
    if not cam then return headPart.Position end

    local targetPos = headPart.Position
    if not State.Prediction then return targetPos end

    local camPos = cam.CFrame.Position
    local v = math.max(bulletSpeed or cachedBulletSpeed or 2000, 1)
    local g = bulletGravity or cachedBulletGravity or 0

    local rawPing = getPing() or 0
    local pingInSec = (rawPing > 5) and (rawPing / 1000.0) or rawPing
    local pingSec = pingOffsetSec or (pingInSec + 0.0166)

    local now = os.clock()
    local lastTime = lastTickTime[target] or (now - 0.016)
    local dt = math.clamp(now - lastTime, 0.001, 0.1)
    lastTickTime[target] = now

    if not velocityHistory[target] then velocityHistory[target] = {} end
    local hist = velocityHistory[target]

    local currentVel = rootPart.AssemblyLinearVelocity
    table.insert(hist, currentVel)
    if #hist > 5 then table.remove(hist, 1) end

    local avgVel = Vector3.new(0, 0, 0)
    for _, vel in ipairs(hist) do
        avgVel = avgVel + vel
    end
    avgVel = avgVel / #hist

    local targetAccel = Vector3.new(0, 0, 0)
    if #hist >= 2 then
        local prevVel = hist[#hist - 1]
        targetAccel = (currentVel - prevVel) / dt
        if targetAccel.Magnitude > 60 then
            targetAccel = targetAccel.Unit * 60
        end
    end

    -- คำนวณกระสุนเส้นตรง (Straight Bullet / High Speed)
    if g < 0.01 then
        local predictedPos = targetPos
        for iter = 1, 4 do
            local dist = (predictedPos - camPos).Magnitude
            local tof = (dist / v) + pingSec
            predictedPos = targetPos + (avgVel * tof) + (0.5 * targetAccel * (tof * tof))
        end
        return predictedPos
    end

    -- คำนวณย้อยตามแรงโน้มถ่วง (Bullet Drop / Gravity)
    local predictedPos = targetPos
    local lastTanTheta = 0

    for iter = 1, 6 do
        local diff = predictedPos - camPos
        local flatDiff = Vector3.new(diff.X, 0, diff.Z)
        local horizDist = flatDiff.Magnitude
        local vertDist = diff.Y

        if horizDist < 0.001 then
            local tofVert = math.abs(vertDist) / v + pingSec
            predictedPos = targetPos + avgVel * tofVert + 0.5 * targetAccel * (tofVert * tofVert)
            break
        end

        local v2 = v * v
        local disc = v2 * v2 - g * (g * horizDist * horizDist + 2 * vertDist * v2)

        if disc < 0 then
            lastTanTheta = (vertDist + 0.5 * g * (horizDist / v)^2) / math.max(horizDist, 0.001)
            break
        end

        local sqrtDisc = math.sqrt(disc)
        local tanTheta = (v2 - sqrtDisc) / (g * horizDist)
        lastTanTheta = tanTheta

        local cosTheta = 1.0 / math.sqrt(1.0 + tanTheta * tanTheta)
        local tof = (horizDist / (v * cosTheta)) + pingSec

        local newPos = targetPos + avgVel * tof + 0.5 * targetAccel * (tof * tof)
        if (newPos - predictedPos).Magnitude < 0.01 then
            predictedPos = newPos
            break
        end
        predictedPos = newPos
    end

    local diff = predictedPos - camPos
    local flatDiff = Vector3.new(diff.X, 0, diff.Z)
    local finalHorizDist = flatDiff.Magnitude

    if finalHorizDist < 0.01 then return predictedPos end
    return camPos + (flatDiff.Unit * finalHorizDist) + Vector3.new(0, finalHorizDist * lastTanTheta, 0)
end


-- 11. ESP Management
local function createESP(player, character)
    local head = character:FindFirstChild("Head") or character:WaitForChild("Head", 2)
    if not head then return end

    local highlight = Instance.new("Highlight")
    highlight.Name = "ESP_Highlight"
    highlight.FillColor = Color3.fromRGB(180, 0, 255)
    highlight.OutlineColor = Color3.fromRGB(255, 255, 0)
    highlight.FillTransparency = 0.15
    highlight.OutlineTransparency = 0
    highlight.Enabled = false
    highlight.Parent = character

    local nameTag = Instance.new("BillboardGui")
    nameTag.Name = "ESP_NameTag"
    nameTag.Adornee = head
    nameTag.Size = UDim2.new(0, 150, 0, 30)
    nameTag.StudsOffset = Vector3.new(0, 2, 0)
    nameTag.AlwaysOnTop = true
    nameTag.Enabled = false

    local textLabel = Instance.new("TextLabel")
    textLabel.Size = UDim2.new(1, 0, 1, 0)
    textLabel.BackgroundTransparency = 1
    textLabel.Text = player.Name
    textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    textLabel.TextSize = 14
    textLabel.Font = Enum.Font.SourceSansBold
    textLabel.TextStrokeTransparency = 0
    textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    textLabel.Parent = nameTag

    nameTag.Parent = character

    espCache[player] = {
        Highlight = highlight,
        NameTag = nameTag,
        Character = character
    }
end

local function removeESP(player)
    if espCache[player] then
        if espCache[player].Highlight then espCache[player].Highlight:Destroy() end
        if espCache[player].NameTag then espCache[player].NameTag:Destroy() end
        espCache[player] = nil
    end
end

local function processESP()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            local character = player.Character
            
            if character and character:FindFirstChild("HumanoidRootPart") then
                if not espCache[player] or espCache[player].Character ~= character then
                    removeESP(player)
                    createESP(player, character)
                end

                local cache = espCache[player]
                if cache then
                    local isEnemy = isValidEnemy(player)
                    cache.Highlight.Enabled = State.ESP and isEnemy
                    cache.NameTag.Enabled = State.ESPName and isEnemy
                end
            else
                removeESP(player)
            end
        end
    end
end

Players.PlayerRemoving:Connect(removeESP)

task.spawn(function()
    while true do
        processESP()
        task.wait(0.1)
    end
end)

-- 12. Main Camera Engine (Fast Lock + Anti-Shake)
local lastTargetCFrame = nil

RunService:UnbindFromRenderStep("AimbotCameraUpdate")
RunService:BindToRenderStep("AimbotCameraUpdate", Enum.RenderPriority.Camera.Value + 1, function(deltaTime)
    local cam = getCamera()
    if not cam then return end

    if FOVCircle then
        FOVCircle.Position = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)  
        FOVCircle.Radius = State.FOV  
        FOVCircle.Visible = State.ShowFOV  
    end
      
    if not State.Aimbot then 
        currentTarget = nil
        lastTargetCFrame = nil
        return 
    end
    
    currentTarget = getClosestTarget()  
    if currentTarget then  
        local rawTargetPos = getPredictedPosition(currentTarget)  
        if rawTargetPos then  
            local camPos = cam.CFrame.Position
            local targetCFrame = CFrame.new(camPos, rawTargetPos)
            
            -- ปรับคำนวณการหมุนกล้องแบบตอบสนองไว (Fast Response)
            if State.Smoothness >= 0.95 then
                cam.CFrame = targetCFrame
            else
                -- สูตร Lerp แบบปรับสเกลเวลา ล็อคเข้าเป้าไวขึ้น 3 เท่าโดยไม่สั่นสะดุด
                local smoothRate = math.clamp((1 - State.Smoothness) * 40, 5, 50)
                local lerpFactor = math.clamp(deltaTime * smoothRate, 0.05, 1)
                cam.CFrame = cam.CFrame:Lerp(targetCFrame, lerpFactor)
            end
            lastTargetCFrame = cam.CFrame
        end  
    else
        lastTargetCFrame = nil
    end
end)

-- 13. Custom GUI Construction
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SnyX_CustomAimbotUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = localPlayer:WaitForChild("PlayerGui")

local LogoButton = Instance.new("TextButton")
LogoButton.Name = "LogoButton"
LogoButton.Size = UDim2.new(0, 50, 0, 50)
LogoButton.Position = UDim2.new(0.05, 0, 0.2, 0)
LogoButton.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
LogoButton.BorderSizePixel = 0
LogoButton.Text = "SnyX"
LogoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoButton.TextSize = 16
LogoButton.Font = Enum.Font.SourceSansBold
LogoButton.Active = true
LogoButton.Draggable = true
LogoButton.Visible = true
LogoButton.Parent = ScreenGui

local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(0, 10)
LogoCorner.Parent = LogoButton

local LogoStroke = Instance.new("UIStroke")
LogoStroke.Color = Color3.fromRGB(180, 0, 255)
LogoStroke.Thickness = 2
LogoStroke.Parent = LogoButton

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 290, 0, 360)
MainFrame.Position = UDim2.new(0.1, 0, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(45, 45, 45)
MainStroke.Thickness = 1
MainStroke.Parent = MainFrame

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 35)
TitleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 8)
TitleCorner.Parent = TitleBar

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(0.7, 0, 1, 0)
TitleText.Position = UDim2.new(0.04, 0, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "ADVANCED AIMBOT SYSTEM"
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.TextSize = 13
TitleText.Font = Enum.Font.SourceSansBold
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.Parent = TitleBar

local ScrollFrame = Instance.new("ScrollingFrame")
ScrollFrame.Size = UDim2.new(1, -10, 1, -45)
local ScrollFrame = Instance.new("ScrollingFrame")
ScrollFrame.Size = UDim2.new(1, -10, 1, -45)
ScrollFrame.Position = UDim2.new(0, 5, 0, 40)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.BorderSizePixel = 0
ScrollFrame.ScrollBarThickness = 3
ScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(100, 100, 100)
ScrollFrame.Parent = MainFrame

local UIList = Instance.new("UIListLayout")
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0, 6)
UIList.Parent = ScrollFrame

UIList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, UIList.AbsoluteContentSize.Y + 10)
end)

local layoutOrder = 0

local function createToggle(text, initialState, callback)
    layoutOrder = layoutOrder + 1
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 32)
    frame.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    frame.BorderSizePixel = 0
    frame.LayoutOrder = layoutOrder
    frame.Parent = ScrollFrame

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = frame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, -10, 1, 0)
    label.Position = UDim2.new(0.04, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.TextSize = 11
    label.Font = Enum.Font.SourceSans
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 48, 0, 20)
    btn.Position = UDim2.new(1, -54, 0.5, -10)
    btn.BackgroundColor3 = initialState and Color3.fromRGB(50, 150, 50) or Color3.fromRGB(150, 50, 50)
    btn.Text = initialState and "ON" or "OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 10
    btn.Font = Enum.Font.SourceSansBold
    btn.Parent = frame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 4)
    btnCorner.Parent = btn

    local isToggled = initialState
    btn.MouseButton1Click:Connect(function()
        isToggled = not isToggled
        btn.Text = isToggled and "ON" or "OFF"
        btn.BackgroundColor3 = isToggled and Color3.fromRGB(50, 150, 50) or Color3.fromRGB(150, 50, 50)
        callback(isToggled)
    end)
end

local function createSlider(text, min, max, default, step, callback)
    layoutOrder = layoutOrder + 1
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 48)
    frame.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    frame.BorderSizePixel = 0
    frame.LayoutOrder = layoutOrder
    frame.Parent = ScrollFrame

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = frame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 0, 20)
    label.Position = UDim2.new(0.04, 0, 0, 3)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.TextSize = 11
    label.Font = Enum.Font.SourceSans
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local valLabel = Instance.new("TextLabel")
    valLabel.Size = UDim2.new(0.3, 0, 0, 20)
    valLabel.Position = UDim2.new(0.65, 0, 0, 3)
    valLabel.BackgroundTransparency = 1
    valLabel.Text = tostring(default)
    valLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
    valLabel.TextSize = 11
    valLabel.Font = Enum.Font.SourceSansBold
    valLabel.TextXAlignment = Enum.TextXAlignment.Right
    valLabel.Parent = frame

    local sliderBar = Instance.new("TextButton")
    sliderBar.Size = UDim2.new(0.92, 0, 0, 6)
    sliderBar.Position = UDim2.new(0.04, 0, 0, 30)
    sliderBar.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    sliderBar.Text = ""
    sliderBar.AutoButtonColor = false
    sliderBar.Parent = frame

    local barCorner = Instance.new("UICorner")
    barCorner.CornerRadius = UDim.new(0, 3)
    barCorner.Parent = sliderBar

    local sliderFill = Instance.new("Frame")
    local initialRatio = math.clamp((default - min) / (max - min), 0, 1)
    sliderFill.Size = UDim2.new(initialRatio, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderBar

    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(0, 3)
    fillCorner.Parent = sliderFill

    local dragging = false
    local moveConnection, releaseConnection

    local function updateSlider(input)
        local barAbsPos = sliderBar.AbsolutePosition.X
        local barAbsSize = sliderBar.AbsoluteSize.X
        local mousePos = input.Position.X
        local ratio = math.clamp((mousePos - barAbsPos) / barAbsSize, 0, 1)
        local rawValue = min + (ratio * (max - min))
        local steppedValue = math.floor(rawValue / step + 0.5) * step
        steppedValue = math.clamp(steppedValue, min, max)

        sliderFill.Size = UDim2.new((steppedValue - min) / (max - min), 0, 1, 0)
        valLabel.Text = tostring(steppedValue)
        callback(steppedValue)
    end

    sliderBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            updateSlider(input)

            if moveConnection then moveConnection:Disconnect() end
            if releaseConnection then releaseConnection:Disconnect() end

            moveConnection = UserInputService.InputChanged:Connect(function(moveInput)
                if dragging and (moveInput.UserInputType == Enum.UserInputType.MouseMovement or moveInput.UserInputType == Enum.UserInputType.Touch) then
                    updateSlider(moveInput)
                end
            end)

            releaseConnection = UserInputService.InputEnded:Connect(function(releaseInput)
                if releaseInput.UserInputType == Enum.UserInputType.MouseButton1 or releaseInput.UserInputType == Enum.UserInputType.Touch then
                    dragging = false
                    if moveConnection then moveConnection:Disconnect() end
                    if releaseConnection then releaseConnection:Disconnect() end
                end
            end)
        end
    end)
end

-- UI Controls Construction
createToggle("Enable Aimbot", State.Aimbot, function(v) State.Aimbot = v end)
createToggle("Show FOV Circle", State.ShowFOV, function(v) State.ShowFOV = v end)
createToggle("Team Check", State.TeamCheck, function(v) State.TeamCheck = v end)
createToggle("Wall Check", State.WallCheck, function(v) State.WallCheck = v end)
createToggle("Prediction Engine", State.Prediction, function(v) State.Prediction = v end)
createToggle("Auto Detect Weapon Speed", State.AutoBulletSpeed, function(v) 
    State.AutoBulletSpeed = v 
    updateWeaponStats()
end)
createToggle("ESP Wallhack", State.ESP, function(v) State.ESP = v end)
createToggle("ESP Username", State.ESPName, function(v) State.ESPName = v end)

createSlider("Manual Bullet Speed", 500, 10000, State.BulletSpeed, 100, function(v) 
    State.BulletSpeed = v 
    updateWeaponStats()
end)
createSlider("Smoothness", 0.05, 1, State.Smoothness, 0.05, function(v) State.Smoothness = v end)
createSlider("Aimbot FOV Range", 50, 500, State.FOV, 10, function(v) State.FOV = v end)

LogoButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
